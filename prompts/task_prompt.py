task_prompt = {}
task_prompt['QA']="""
你是一个专业的复杂问答数据合成专家，擅长将知识图谱中的多跳关系转化为自然、流畅的推理问题。
请基于提供的知识图谱子图或路径信息，及相关载体证据信息(图片/表格/公式等)，生成一个高质量的多跳推理问答对。
 
### 知识图谱子图或者路径信息:
{{{graph_context}}}
- **实体节点**：知识图谱中的人物、地点、模型、任务等真实实体。  
- **载体节点**（Table、Image、Figure 等）：用于提供前置实体的相关上下文，**不直接作为答案**。  
- **关系**：`belong_to、locate_at` 等仅表示归属或定位，不参与推理链的逻辑推导。
 
### 生成要求
1. **问题设计原则**：
   - 问题应通过多个实体间的关系推导得出，体现跨实体、多跳逻辑的复杂性，避免多问句连续发问
   - 问题必须自然流畅，避免机械式提问，而是要用自然的语言表达
   - 问题必须情境包装，而非显示地暴露推理步骤，严禁显示答案名称或是答案中含有的关键字词
   - 若图表或图片中包含数值、趋势、比较关系等信息，请注意所生成的问题需要加入足够的载体相关上下文信息作为context，否则模型无法进行推理作答，使得模型必须“结合图表理解”才能得出正确答案，你需要把足够的信息加入question中作为上下文
   - **当路径中包含图片或表格节点时**：可以设计与该载体内容相关的推理类型，例如数值比较、趋势变化等
   - 问题应基于载体所展示的信息，但最终答案应仍为与该载体相连的实体名称或其明确属性。

 
2. **推理过程要求**：
   - 你生成的推理链条步骤可以利用到路径上每个实体的额外属性信息
   - 你合成的推理链条要连贯，模拟真实的人类推理过程，运用自然逻辑连接词，不要显式标注三元组关系。要写成连贯的思考过程，而不是机械地列出实体和关系。  
   - 你生成的内容中，不要带有多余信息，例如"根据知识图谱"、"根据某关系"、"根据某实体信息"、"完整推理链条如下"、"根据某某图片/表格"等字样
   - **当终点是图表/表格/公式时**：该节点及其相关属性只是载体证据，用来推导出**载体前一个实体**的确切名称或者其特定属性值
   - belong_to, locate_at, related_to 等关系也只是为了表明这种所属关系，不作为实际推理过程中的推理关系，需要分析相关载体内容对于前一个实体的
   - 当路径中包含图表/图片/表格节点时，应将其内容作为辅助证据，用于支撑或限定推理方向。
 
3. **答案要求**：
   - 若路径终点含"图片/表格/公式"等载体证据：答案应该是**载体前一个实体**的确切名称（如路径 A→B→[图表]，答案=B或其属性值），不要篇幅过长
   - 严禁将载体名称或其相关属性作为答案
   - 答案要唯一且具体，避免模糊或多义
   - 答案最好不要是数值类，可以是需要多步推理之后得到的趋势变化、图表中的相关实体名称等

4. **特殊处理**：
   - 若路径仅含单实体节点：
     • 普通实体：基于属性生成问答
 
### 输出规范 
- 严格返回JSON格式，无任何额外文本（包括'```json'等标记）
- 字段要求：
  "question": 复杂的自然语言问题
  "answer": 简洁且具体唯一的答案
  "reasoning_path": 问题设计的推理过程，是一个CoT思考或推理过程，要求自然连贯
"""


task_prompt['TF']="""
以下是转换成中文提示词的内容，保持了结构和位置，但去除了LaTeX格式：

你是一个专业的复杂问答数据合成专家，擅长将知识图谱中的单跳和多跳关系转化为高度隐蔽且具有挑战性的真假问题。

请根据提供的知识图谱子图或路径信息生成一个高质量的真假问题：

**{graph_context}**

**生成要求：**

1. 问题必须是**复杂且精巧的**，通过追溯多个实体之间的关系链，全面利用知识图谱中的多个实体和关系，避免简单直接的单一关系陈述。
2. 问题必须是**真假问题**，并且答案明确（真/假）。判断必须严格符合知识图谱中的逻辑关系和属性信息，不得含糊。
3. 问题必须是**自然流畅的**，避免机械的表达（例如直接列出实体和关系），采用与现实交流场景一致的自然语言表达。
4. 问题可以**根据具体的现实场景进行包装**（例如学术研究、日常应用、专业实践），而不是明确暴露推理逻辑，并且绝不可直接透露核心判断点。
5. 推理基础可以充分利用路径中每个实体的额外属性信息（例如类型、特征、范围）来增加问题的复杂性和隐蔽性。
6. 问题中的隐性推理链必须是**连贯的**，模拟真实的人类推理过程，具有自然的逻辑连接（例如因果关系、条件关系、联想关系），而不是机械地拼接实体-关系对。
7. 答案必须是**准确且唯一的判断**（真/假），严格符合提供的知识图谱路径或子图的逻辑一致性。
8. 生成的内容不得包含冗余信息，例如“根据知识图谱”、“基于某个关系”或对知识图谱本身的显式引用。
9. 如果提供的路径或子图仅包含一个实体节点，请根据该实体的核心属性（例如范围、功能、特征）生成一个合理且具有挑战性的判断问题，并确保答案逻辑有效。
10. 最终输出必须严格遵循JSON格式，且JSON结构外不得包含任何额外文本。

请生成一个符合要求的问答对，并以JSON格式返回：

```
{
    "question": "生成的复杂真假问题，具有自然的场景包装",
    "answer": "准确的判断（真/假）",
    "reasoning_path": "问题设计的连贯思考过程，解释问题如何整合多实体关系/属性，并说明为什么答案为真/假——必须自然且符合真实推理逻辑"
}
```

"""

# ==========================
# 一、表格问答 & 表格与文本
# ==========================
task_prompt["single_table"] = """
你是一个专业的复杂问答数据合成专家，擅长基于结构化表格设计自然的问题和答案。

### 语言与输出约束
- 所有生成内容使用**简体中文**。
- `question` 是自然的单句问句，不要包含编号、引号或多问句串联。
- 问题与推理过程中避免使用“表格、字段、列名、行号”等技术性词语。

### 输入
- 单个表格的结构与内容：
{{{graph_context}}}

### 任务类型
单表格问答：只使用这一张表格的信息生成问题和答案。

### 生成要求

1. 问题设计
   - 围绕表格中的实体行及其属性**设计问题。
   - 问题可以是一步推理，但不要退化为机械地询问“某个单元格是什么”。
   - 在问题中加入轻微情境或任务背景，例如决策、选择、推荐、判断等，使问句更加贴近真实应用场景。
   - 问题必须仅依赖该表格即可回答，不涉及任何外部知识、其他文本、公式或图表。
   - 问题中不要显式说“根据上面的表、从这张表来看”等。

2. 推理过程（reasoning_path）
   - 以**自然语言**描述如何理解问题并在表格内容中找到答案，可以包括：
     - 先确定问题涉及的对象；
     - 再根据相关属性进行查找或简单比较；
     - 最终锁定唯一结果。
   - 不要提及“第几列”“某字段”“某个单元格”等元信息，只描述内容本身。
   - 推理不少于 2 句，避免简单复述问题或直接给出结论。

3. 答案要求（answer）
   - 答案必须是该表格中**某一行实体的确切名称**，或该实体的一个**明确属性值**。
   - 答案要**简洁、具体、唯一**，能够在表格中被唯一定位。
   - 不要输出模糊描述、范围、列表或多选答案。

4. 输出格式
   - 严格返回 JSON 格式，无任何额外文本（不要包含 ```json 代码块标记）：
     {
       "question": "...",
       "answer": "...",
       "reasoning_path": "..."
     }
"""
task_prompt["single_table_mutihop"] = """
你是一个专业的复杂问答数据合成专家，擅长在单张表格上构造多步推理问题。
### 语言与输出约束
- 所有生成内容使用简体中文。
- `question` 是单句或少量复句组成的自然问句，不含编号或引号。
- 问题与推理过程中避免出现“表格、字段、列名、行号”等技术性描述。

### 输入
- 单个表格的结构与内容：
{{{graph_context}}}

### 任务类型
单表多跳问答：只使用这一张表格，但需要通过至少两步推理才能得到答案。

### 生成要求

1. 问题设计
   - 问题必须包含**两步及以上**的隐含推理，例如：
     - 先根据某些条件筛选出候选实体（如时间、类别、数值区间等）；
     - 再在候选实体中按另一属性进行比较、排序或二次过滤；
     - 最终确定唯一对象或属性。
   - 避免“一眼就能从单个字段直接读出答案”的简单提问。
   - 合理引入决策、排除、最优/最差选择等场景，使推理链条自然合理。
   - 问句中不要直接说明“先筛选再排序”这类显式推理步骤，而是通过条件描述体现复杂度。

2. 推理过程（reasoning_path）
   - 用自然语言串联多步思考，建议3～5句：
     - 第一步说明如何依据某些信息缩小候选范围；
     - 第二步说明如何利用另一类属性进一步比较或筛选；
     - 如有需要，可再进行一轮判断，最终锁定答案。
   - 不要显式提到“表格”“列名”“行号”等，只用内容含义来描述信息（如“价格更低”“发布时间更晚”）。

3. 答案要求（answer）
   - 答案必须是表格中某一行对应的实体名称，或该实体某个**确定属性值**。
   - 答案是多步推理的终点，且在表中**唯一可定位**。
   - 不要生成模棱两可或多实体均满足条件的情况。

4. 输出格式
   - 严格返回 JSON，仅包含三个字段：
     {
       "question": "...",
       "answer": "...",
       "reasoning_path": "..."
     }
"""
task_prompt["single_table_single_text"]= """
你是一个专业的复杂问答数据合成专家，擅长综合结构化表格与短文本信息构造问题。

### 语言与输出约束
- 所有生成内容使用**简体中文**。
- 问题与推理过程避免使用“chunk、字段、表格”等技术性词语。

### 输入
{{{graph_context}}}

### 任务类型
单表 + 单文本问答：问题必须**同时用到该表格和这段文本**的信息，任一部分缺失都无法回答。

### 生成要求

1. 问题设计
   - 文本通常提供：背景设定、概念定义、业务规则、阈值或分档标准等抽象信息。
   - 表格提供：具体实体及其数值或属性。
   - 设计问题时：
     - 先从文本中抽取关键条件、规则或判定标准；
     - 再将这些条件应用到表格中的实体上进行筛选、过滤或比较；
     - 最终确定唯一答案。
   - 问题必须保证：仅看表格无法回答，仅看文本也无法回答，必须**结合两者**。
   - 问句不要显式说“根据上面的文字描述和表格”，而是用自然业务场景表述。

2. 推理过程（reasoning_path）
   - 清晰体现“先理解文本，再使用表格”的顺序，例如：
     - 先解释文本中给出的规则或阈值；
     - 然后说明如何将这些条件应用到表格中的实体属性上；
     - 再描述筛选或比较过程，最终锁定唯一对象。
   - 使用自然语言，不出现“文本 chunk”“表格字段”等术语。

3. 答案要求（answer）
   - 答案必须是表格中某个实体的**名称**或该实体的某个**明确属性值**。
   - 该实体在结合文本条件后是唯一满足要求的对象。
   - 不要输出多个备选答案或开放式描述。

4. 输出格式
   - 严格返回 JSON，仅包含：
     {
       "question": "...",
       "answer": "...",
       "reasoning_path": "..."
     }
"""
task_prompt["single_table_multi_text_qa"]= """
你是一个专业的复杂问答数据合成专家，擅长综合单个表格与多段文本构造复杂问题。

### 语言与输出约束
- 所有生成内容使用**简体中文**。
- 问题与推理中避免使用“chunk、段落编号、表格字段”等技术性说法。

### 输入
- 单个表格的结构与内容：
{{{graph_context}}}

### 任务类型
单表 + 多文本问答：问题必须同时用到该表格和**多段文本信息**，仅依赖其中任何一部分都无法回答。

### 生成要求

1. 问题设计
   - 多个文本段分别提供不同的规则、前提条件、业务策略或例外情况。
   - 表格提供实体及其具体数值或属性。
   - 问题设计应满足：
     - 至少从两段及以上文本中抽取条件（例如一段给出总体标准，另一段给出特殊优先级或限制）；
     - 将这些条件逐步应用于表格实体进行过滤、对比或排序；
     - 最终确定唯一实体或属性值。
   - 问句本身只描述综合后的条件，不需要暴露“来自多段文本”的事实。

2. 推理过程（reasoning_path）
   - 合理分步描述推理顺序，例如：
     - 先根据某段文本确定基本入围条件；
     - 再根据另一段文本添加额外限制或优先级规则；
     - 然后在表格中按照这些组合条件对实体进行筛选或比较；
     - 最终选出唯一满足所有条件的实体。
   - 使用自然语言，不使用“chunk1 / chunk2”这类标记。

3. 答案要求（answer）
   - 答案是表格中唯一满足所有文本条件的实体名称或该实体的某个属性值。
   - 该答案可以在表格中被**唯一定位**。
   - 不允许多实体同时满足条件。

4. 输出格式
   - 严格返回 JSON，仅包含：
     {
       "question": "...",
       "answer": "...",
       "reasoning_path": "..."
     }
"""
task_prompt["multi_table"] = """
你是一个专业的复杂问答数据合成专家，擅长在多张表格之间建立关联并设计问答。

### 语言与输出约束
- 所有生成内容使用**简体中文**。
- 避免在问题和推理中使用“表 A、表 B、字段、主键”等技术性术语。

### 输入
- 多个表格的结构与内容：
{{{graph_context}}}

### 任务类型
多表问答：利用两张或多张表格之间的关联信息进行推理（不强制多跳，但必须跨表）。

### 生成要求

1. 问题设计
   - 问题必须用到**至少两张不同表格**中的信息。
   - 常见模式：
     - 在一张表中找到某个中间实体或标识（如用户、产品编号、项目名称）；
     - 再到另一张表中查找与该实体相关的其他属性或结果。
   - 不能仅在单一表格内完成全部推理。
   - 问题应体现在语义上需要“先知道谁/什么，再了解它的其他信息”的跨表逻辑，但不用显式说明“去另一张表”。

2. 推理过程（reasoning_path）
   - 描述清楚从哪类信息出发，如何跨表关联：
     - 先在一组记录中确定目标实体或中间标识；
     - 再利用该标识在另一组记录中查找其附加属性、状态或指标；
     - 最终得到答案。
   - 使用内容含义来指代信息，例如“先确定这位客户，再查看他在另一项业务中的表现”，而不是“先在第一张表，再到第二张表”。

3. 答案要求（answer）
   - 答案是多张表格中某一行对应实体的名称，或该实体的某个属性值。
   - 答案必须唯一，且能在给定表格中准确定位。
   - 不允许出现“有多个可能答案”或开放式结论。

4. 输出格式
   - 严格返回 JSON，字段为：
     {
       "question": "...",
       "answer": "...",
       "reasoning_path": "..."
     }
"""
task_prompt["multi_table_multihop"] = """
你是一个专业的复杂问答数据合成专家，擅长在多张关联表格之间构造多跳推理链条。

### 语言与输出约束
- 所有生成内容使用**简体中文**。
- 问题与推理过程避免出现“表1/表2、字段名、主键”等技术性用语。

### 输入
- 多个通过某些字段或实体相互关联的表格：
{{{graph_context}}}

### 任务类型
多表多跳问答：基于多张表格中相互连接的实体，进行**多步前序推理**（至少 2 跳）。

### 生成要求

1. 问题设计
   - 问题必须显式依赖**至少两张表格**，并包含多步推理：
     - 例如：先在一组记录中根据条件确定某类对象；
     - 再使用它在另一组记录中查找相关记录或属性；
     - 可能还需要依据第三张记录集的内容进行进一步筛选或比较。
   - 不允许通过一次简单关联就直接得到答案，必须体现连续的“缩小范围→再判断→最终确定”的链条。
   - 问题语句应通过描述条件和场景体现复杂度，而不是直白讲出“第一步、第二步”。

2. 推理过程（reasoning_path）
   - 要呈现清晰的多阶段思考，建议 3～6 句：
     - 第一步：说明如何利用一组信息缩小候选范围；
     - 第二步：说明如何借助另一组相关记录进一步筛选或查找附加属性；
     - 如有需要，第三步再根据额外条件做最终对比或排序；
     - 最后给出唯一答案。
   - 只用实体内容和关系来描述推理，不提“表格”“字段名”“列”等。

3. 答案要求（answer）
   - 答案是所有表格中某个实体的名称或属性值，是多跳推理链的终点。
   - 该实体在表格中必须**唯一可确定**。
   - 不要生成“多个实体都满足条件”的情况。

4. 输出格式
   - 严格返回 JSON，仅包含：
     {
       "question": "...",
       "answer": "...",
       "reasoning_path": "..."
     }
"""
task_prompt["multi_table_multi_text"] = """
你是一个专业的复杂问答数据合成专家，擅长结合多张表格与多段文本构造复杂问答。

### 语言与输出约束
- 所有生成内容使用**简体中文**。
- 问题与推理过程中避免出现“chunk、表1、表2、字段名”等技术性术语。

### 输入
{{{graph_context}}}

### 任务类型
多表 + 多文本问答：问题必须**同时用到多张表格和至少两个不同文本片段**。

### 生成要求

1. 问题设计
   - 文本提供多条规则、约束、背景、策略或阈值：
     - 不同段落可能规定不同条件、优先级或例外情况。
   - 多张表格提供关于实体的结构化信息（身份、属性、数值、行为记录等）。
   - 问题必须满足：
     - 从**至少两段文本**中提取互补或叠加的条件；
     - 在**至少两张表格**之间跳转查找或对比实体；
     - 仅依赖任意一部分信息（单一表或单一段文本）都无法回答。
   - 问句用自然业务语言描述综合条件，不要提到“某段文字”“某张表”。

2. 推理过程（reasoning_path）
   - 体现“多文本 → 多表”的逐步推理流程，例如：
     - 先从若干文本段落中提取若干条规则或优先级；
     - 再在一组记录中依据这些规则筛选第一批候选实体；
     - 然后在另一组或多组记录中查找这些候选实体的其他属性或表现；
     - 按照文本中的进一步要求进行过滤或比较；
     - 最终得到唯一满足全部条件的实体。
   - 全程使用自然语言描述，不出现“chunk”“表1表2”等术语。

3. 答案要求（answer）
   - 答案是表格中某个实体的名称，或该实体的某个明确属性值。
   - 必须是综合多表信息和多段文本约束之后的**唯一结果**，可以在表中唯一定位。
   - 不要产生多答案或模糊结论。

4. 输出格式
   - 严格返回 JSON，仅包含：
     {
       "question": "...",
       "answer": "...",
       "reasoning_path": "..."
     }
"""


# ==========================
# 二、文档问答（文本为主，答案仍落在表格实体）
# ==========================
task_prompt["single_text"] = """
你是一名专业的复杂问答数据合成专家，擅长基于单段文本构造问答，并将答案严格对齐到给定表格中的实体。

### 输入
{{{graph_context}}}

### 任务类型
单文本问答：仅依赖这一段文本就能确定答案，但最终答案必须使用候选表格中的实体或其属性形式表达。

### 生成要求
1. 问题设计：
   - 围绕文本中的关键信息设计自然问题，可以涉及人物、事件、时间、地点、概念、关系等。
   - 仅凭这一段文本即可推断出对应的对象，但输出时必须使用表格中给出的实体名称或属性值，而不是自由描述。
   - 不要直接复述文本中的句子作为问题，要有一定的改写和抽象。

2. 推理过程：
   - 在 reasoning_path 中，描述如何从文本中识别关键线索（比如名字、时间、特征），再将这些线索与表格中的实体或属性进行匹配。
   - 推理可以是单步，但要体现“从文本理解 → 映射到表格实体”的过程，而不是简单说“在表中查找”。

3. 答案要求：
   - 答案必须是候选表格中某一行实体的名称，或该实体的一项明确属性值。
   - 该答案必须能在表格中唯一定位（不存在多行同时符合）。
   - 答案不要是一句话总结或模糊描述，只输出具体名称或具体属性值。

4. 输出格式：
   - 严格返回 JSON，仅包含以下字段：
     {
       "question": "...",
       "answer": "...",
       "reasoning_path": "..."
     }
"""
task_prompt["multi_text"] = """
你是一名专业的复杂问答数据合成专家，擅长从多段文本中整合信息，并将答案对齐到表格实体。

### 输入
{{{graph_context}}}

### 任务类型
多文本问答：需要综合多个文本 chunk 中的互补信息才能确定答案。

### 生成要求
1. 问题设计：
   - 问题必须需要从至少两个不同文本 chunk 中获取信息，任何单独一段都不足以确定唯一答案。
   - 不同文本可以分别提供关于同一实体的不同线索。
   - 最终答案仍必须映射为表格中的实体名称或属性值，不允许出现表格外的新实体。

2. 推理过程：
   - 在 reasoning_path 中，描述：
     - 先从某段文本中抽取第一条线索；
     - 再从另一段文本中抽取第二条线索；
     - 然后将这些线索与表格中的实体属性逐一对照，找到唯一匹配的实体。
   - 不要使用“chunk1 / chunk2”等技术性称呼，只用自然语言描述不同段落中提供的信息。

3. 答案要求：
   - 答案是候选表格中唯一同时满足多段文本线索的实体名称或其属性值。
   - 答案必须可以在表格中唯一定位。

4. 输出格式：
   - 严格返回 JSON，仅包含：
     {
       "question": "...",
       "answer": "...",
       "reasoning_path": "..."
     }
"""
task_prompt["single_text_multihop"] = """
你是一名专业的复杂问答数据合成专家，擅长在单段文本上构造多跳推理问题，并将答案落在表格实体上。

### 输入
{{{graph_context}}}

### 任务类型
单文本多跳问答：所有线索都在同一段文本中，但需要至少两步以上推理才能确定答案。

### 生成要求
1. 问题设计：
   - 问题必须要求模型进行多步思考，例如：
     - 先理解多个人物或事件之间的关系；
     - 再结合时间、地点、因果、条件等信息；
     - 最终确定目标对象。
   - 禁止那种“文本中直接出现答案实体名称，改写一下就可直接回答”的单跳问题，需要明确的推理链。

2. 推理过程：
   - 在 reasoning_path 中，分步骤描述：
     - 第一步在文本中识别哪些候选对象或中间线索；
     - 第二步基于其他信息（如时间顺序、因果、数量比较等）排除不符合的对象；
     - 最后将得到的对象与表格中的实体名称或属性进行对齐，确定最终答案。
   - 使用自然语言说明推理步骤，不要提“第几句”“第几行”。

3. 答案要求：
   - 答案必须是候选表格中某个实体的名称或属性值，是多跳推理的最终结果。
   - 该实体在表格中可唯一定位。

4. 输出格式：
   - 严格返回 JSON，仅包含：
     {
       "question": "...",
       "answer": "...",
       "reasoning_path": "..."
     }
"""
task_prompt["multi_text_multihop"]= """
你是一名专业的复杂问答数据合成专家，擅长在多段文本上构造多跳推理问题，并将答案对齐到表格实体。

### 输入
{{{graph_context}}}

### 任务类型
多文本多跳问答：需要跨多个文本段落进行多步推理，才能确定答案，并最终映射到表格实体。

### 生成要求
1. 问题设计：
   - 问题必须同时满足：
     - 至少用到两个不同的文本段落；
     - 至少包含两步以上的推理。
   - 可以设计为：
     - 一段文本给出主体或候选对象集合；
     - 另一段文本给出额外的限制条件、结果、偏好或对比信息；
     - 最后通过多步整合锁定唯一实体。
   - 避免“一段文本中已经直接点名实体，另一段只是重复信息”的伪多跳。

2. 推理过程：
   - 在 reasoning_path 中清晰展示跨段落、多步推理的顺序：
     - 先从某段中确定候选范围或关键属性；
     - 再结合另一段中的条件进行筛选、对比或排序；
     - 最终将得到的对象与表格中的实体进行匹配，输出其名称或属性。
   - 不要使用“段落”“chunk”等术语，用内容含义来指代不同信息来源。

3. 答案要求：
   - 答案必须是候选表格中唯一符合所有文本线索和推理条件的实体名称或属性值。
   - 答案可以在表格中唯一定位，不产生歧义。

4. 输出格式：
   - 严格返回 JSON，仅包含：
     {
       "question": "...",
       "answer": "...",
       "reasoning_path": "..."
     }
"""

    # ==========================
    # 三、公式问答 & 公式与文本
    # ==========================
task_prompt["single_formula"] = """
你是一个专业的复杂问答数据合成专家，擅长围绕单个公式设计问题，并将答案对齐到表格实体。

### 输入
{{{graph_context}}}

### 任务类型
单公式问答：仅用一个公式及表格信息即可回答问题，不强制多跳。

### 生成要求
1. 问题设计：
   - 围绕公式的含义、变量之间的关系或简单的代入计算设计问题。
   - 模型可以通过理解公式结构或进行一次计算，就在表格实体中找到符合要求的对象。

2. 推理过程：
   - reasoning_path 描述如何理解公式或代入表格中的数值，并据此筛选或确定某个实体。
   - 不必复杂多跳，但不能只问“某个字母代表什么”这类太简单的问题。

3. 答案要求：
   - 答案是表格中某个实体的名称或该实体基于公式得到的某个属性值。
   - 保证在表格中唯一定位。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""
task_prompt["single_formula_multihop"] = """
你是一个专业的复杂问答数据合成专家，擅长基于单个公式与表格数据构造多跳推理问题。

### 输入
{{{graph_context}}}

### 任务类型
单公式多跳问答：围绕一个公式进行至少两步推理。

### 生成要求
1. 问题设计：
   - 问题需要至少两步思考，例如：
     - 先根据表格中某些属性计算中间变量；
     - 再将中间结果代入公式，得到指标；
     - 最后根据指标进行排序或筛选。
   - 问题必须让模型做不止一次简单代入。

2. 推理过程：
   - reasoning_path 中分步骤展示：
     - 如何从表格中提取所需数据；
     - 如何使用公式计算中间量或指标；
     - 如何根据计算结果在实体间做出选择。

3. 答案要求：
   - 答案是表格中某个实体的名称或该实体的某个计算后属性。
   - 必须是多步推理终点且唯一。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""
task_prompt["single_formula_single_text"] = """
你是一个专业的复杂问答数据合成专家，擅长综合单个公式、一段文本与表格数据构造问题。

### 输入
{{{graph_context}}}

### 任务类型
单公式 + 单文本问答：问题必须同时用到公式、文本和表格。

### 生成要求
1. 问题设计：
   - 文本提供公式适用条件、阈值或额外限制；
   - 公式提供计算方式；
   - 表格提供实体的具体数值。
   - 问题需要先理解文本中的条件，再结合公式对表格实体进行判断。

2. 推理过程：
   - reasoning_path 展现：
     - 如何从文本中抽取条件；
     - 如何将表格数据代入公式计算或比较；
     - 如何据此筛选出唯一实体。

3. 答案要求：
   - 答案是表格中唯一符合文本条件和公式计算结果的实体名称或属性值。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""
task_prompt["single_formula_multi_text"] = """
你是一个专业的复杂问答数据合成专家，擅长综合单个公式、多段文本与表格构造复杂问题。

### 输入
{{{graph_context}}}

### 任务类型
单公式 + 多文本问答：问题必须用到公式和至少两个文本 chunk，并在表格上落地答案。

### 生成要求
1. 问题设计：
   - 不同文本 chunk 提供关于公式使用的不同条件、解释或策略；
   - 公式用于对表格实体计算或判断；
   - 问题需要综合多段文本中的条件后再应用公式到表格数据。

2. 推理过程：
   - reasoning_path 描述：
     - 如何逐段理解文本中的约束；
     - 如何根据这些约束选择公式的使用方式或参数；
     - 然后如何在表格上应用公式，最终确定实体。

3. 答案要求：
   - 答案是表格中唯一符合多段文本条件与公式推理的实体名称或属性值。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""
task_prompt["multi_formula"] = """
你是一个专业的复杂问答数据合成专家，擅长比较或组合多个公式，并将结果映射到表格实体上。

### 输入
{{{graph_context}}}

### 任务类型
多公式问答：问题需要用到不止一个公式，但不强制多跳。

### 生成要求
1. 问题设计：
   - 问题要求模型理解多个公式之间的差异或各自的适用场景；
   - 可以要求根据不同公式的计算结果或指标，对表格中的实体进行简单选择。

2. 推理过程：
   - reasoning_path 描述：
     - 如何选择合适的公式；
     - 或如何对比不同公式下的结果；
     - 最终在实体之间做出判断。

3. 答案要求：
   - 答案是表格中某个实体的名称或属性值。
   - 在给定数据下唯一。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""
task_prompt["multi_formula_multihop"] = """
你是一个专业的复杂问答数据合成专家，擅长串联多个公式和表格数据进行多跳推理。

### 输入
{{{graph_context}}}

### 任务类型
多公式多跳问答：需要在多个公式之间进行多步推理。

### 生成要求
1. 问题设计：
   - 设计至少两步以上的推理，如：
     - 先用公式1计算中间指标；
     - 再用公式2基于该指标进行进一步计算或判断；
     - 最后在实体之间比较结果。
   - 不能只简单应用其中任何一个公式就直接得出答案。

2. 推理过程：
   - reasoning_path 分阶段呈现：
     - 如何按顺序使用不同公式；
     - 如何在每一步中利用表格数据更新判断；
     - 最终锁定实体。

3. 答案要求：
   - 答案是多步公式推理最终确定的、表格中唯一的实体名称或属性值。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""
task_prompt["multi_formula_multi_text"] = """
你是一个专业的复杂问答数据合成专家，擅长综合多个公式、多段文本与表格数据设计复杂推理问题。

### 输入
{{{graph_context}}}

### 任务类型
多公式 + 多文本问答：问题必须用到多个公式和多段文本，并最终在表格实体上落地。

### 生成要求
1. 问题设计：
   - 文本提供规则、策略、不同公式的适用条件等；
   - 多个公式提供不同的计算或评价方式；
   - 表格提供实体数据。
   - 问题需要先从文本中确定应该使用哪些公式以及如何组合，再在表格上应用。

2. 推理过程：
   - reasoning_path 描述：
     - 如何从多段文本中抽取策略；
     - 如何基于这些策略选择和组合公式；
     - 如何在表格数据上执行多步计算和筛选，最终得到唯一实体。

3. 答案要求：
   - 答案是表格中唯一满足所有文本和公式条件的实体名称或属性值。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""

    # ==========================
    # 四、chart 问答 & chart 与文本
    # ==========================
task_prompt["single_chart"] = """
你是一个专业的复杂问答数据合成专家，擅长基于单个图表设计问题，并将答案对齐到表格实体。

### 输入
{{{graph_context}}}

### 任务类型
单 chart 问答：仅用一个图表即可回答问题，不强制多跳。

### 生成要求
1. 问题设计：
   - 围绕图表中的趋势、极值、比较等设计问题。
   - 通过观察图表即可确定哪条线或哪一类满足条件，并映射到表格实体。

2. 推理过程：
   - reasoning_path 描述如何从图表中读取相关信息，并据此选出对应的实体。
   - 不必复杂多步，但也不要变成简单读一个数值。

3. 答案要求：
   - 答案是表格中与图表某条线/某个类别对应的实体名称或属性值。
   - 唯一且明确。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""
task_prompt["single_chart_multihop"]= """
你是一个专业的复杂问答数据合成专家，擅长基于单个图表构造多跳推理问题，并对齐表格实体。

### 输入
{{{graph_context}}}

### 任务类型
单 chart 多跳问答：围绕一个图表进行多步观察和推理。

### 生成要求
1. 问题设计：
   - 问题需要至少两步以上的思考，例如：
     - 先找出某时间段内增长最快的对象；
     - 再在这些对象中比较另一个时间段的表现。
   - 单点读数无法直接得到答案。

2. 推理过程：
   - reasoning_path 描述：
     - 如何依次观察不同时间点或区间；
     - 如何在多次比较后锁定某条线或类别；
     - 再映射到表格实体。

3. 答案要求：
   - 答案是表格中对应的实体名称或属性值，唯一且由多步图表观察得到。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""
task_prompt["single_chart_single_text"]= """
你是一个专业的复杂问答数据合成专家，擅长综合单个图表与单段文本构造问题，并将答案对齐到表格实体。

### 输入
{{{graph_context}}}

### 任务类型
单 chart + 单文本问答：问题必须同时用到图表和文本。

### 生成要求
1. 问题设计：
   - 文本定义“好/差/异常/达标”等标准或额外条件；
   - 图表提供具体数值或趋势；
   - 问题需要先理解文本中的标准，再在图表中找出符合标准的线/类别，并映射到表格实体。

2. 推理过程：
   - reasoning_path 描述：
     - 如何从文本中理解判断规则；
     - 如何在图表中根据规则观察和筛选；
     - 最终选出对应实体。

3. 答案要求：
   - 答案是表格中对应实体的名称或属性值，唯一且同时满足文本条件和图表观察。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""
task_prompt["single_chart_multi_text"]= """
你是一个专业的复杂问答数据合成专家，擅长综合单个图表、多段文本与表格数据构造复杂问题。

### 输入
{{{graph_context}}}

### 任务类型
单 chart + 多文本问答：问题必须用到图表和至少两个不同文本 chunk。

### 生成要求
1. 问题设计：
   - 不同文本 chunk 提供多个维度的条件或不同阶段的标准；
   - 图表提供实体在这些维度上的表现；
   - 问题需要综合多段文本中的要求，在图表中进行多条件筛选。

2. 推理过程：
   - reasoning_path 描述：
     - 如何从多段文本中提取不同的判断标准；
     - 如何按顺序把这些标准应用在图表的观察上；
     - 最终将符合所有标准的线/类别映射到表格实体。

3. 答案要求：
   - 答案是表格中唯一满足所有文本条件和图表表现的实体名称或属性值。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""
task_prompt["multi_chart"] = """
你是一个专业的复杂问答数据合成专家，擅长比较多个图表，并将结果映射到表格实体上。

### 输入
{{{graph_context}}}

### 任务类型
多 chart 问答：需要使用两个或以上图表信息，但不强制多跳。

### 生成要求
1. 问题设计：
   - 问题要求在不同图表之间进行比较或综合判断，例如：
     - 一个图表反映效率，另一个反映成本。
   - 依据多个图表的结果选出最符合描述的实体。

2. 推理过程：
   - reasoning_path 描述如何分别从各图表获取关键信息，并进行综合比较后得到结论。

3. 答案要求：
   - 答案是表格中对应实体的名称或属性值，并且能唯一对应某条线或某类组合表现。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""
task_prompt["multi_chart_multihop"]= """
你是一个专业的复杂问答数据合成专家，擅长在多个图表之间构造多跳推理，并将答案落在表格实体上。

### 输入
{{{graph_context}}}
### 任务类型
多 chart 多跳问答：需要在多个图表之间多步筛选和比较。

### 生成要求
1. 问题设计：
   - 至少两步以上推理，例如：
     - 先在图表1中按某指标挑选候选实体；
     - 再在图表2中按另一个指标进一步筛选或排序；
   - 单独使用任何一个图表都无法直接确定最终答案。

2. 推理过程：
   - reasoning_path 体现跨图表、多阶段的筛选：
     - 先基于某个图表得到候选集合；
     - 再用另一个图表的信息做进一步判断；
     - 最终映射到表格实体。

3. 答案要求：
   - 答案是表格中唯一满足多图表多步条件的实体名称或属性值。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""
task_prompt["multi_chart_multi_text"]= """
你是一个专业的复杂问答数据合成专家，擅长综合多个图表、多段文本与表格数据设计复杂问题。

### 输入
{{{graph_context}}}

### 任务类型
多 chart + 多文本问答：问题必须用到多个图表和多段文本。

### 生成要求
1. 问题设计：
   - 文本提供整体策略、多个指标的优先级或组合规则；
   - 多个图表分别提供不同维度的数据；
   - 问题需要按照文本中的规则，跨多个图表进行筛选和比较。

2. 推理过程：
   - reasoning_path 描述：
     - 如何从文本中提取评估策略；
     - 如何按照策略在多个图表之间跳转进行多步判断；
     - 最终选定某条线/某类并映射到表格实体。

3. 答案要求：
   - 答案是表格中唯一满足文本规则和多个图表表现的实体名称或属性值。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""

    # ==========================
    # 五、交叉生成：表格 & chart & 公式
    # ==========================
task_prompt["single_table_single_chart"]= """
你是一个专业的复杂问答数据合成专家，擅长综合单个表格与单个图表设计问题。

### 输入
{{{graph_context}}}

### 任务类型
单表格 + 单 chart 问答：需要同时利用表格和图表，但不强制多跳。

### 生成要求
1. 问题设计：
   - 表格提供实体及其静态属性；
   - 图表展示这些实体在某维度上的变化或表现；
   - 问题需要同时参考表格和图表的信息才能回答。

2. 推理过程：
   - reasoning_path 描述：
     - 如何在图表中识别出满足条件的线/类别；
     - 如何通过映射在表格中找到对应实体及属性，形成答案。

3. 答案要求：
   - 答案是表格中某个实体的名称或属性值。
   - 必须依赖图表信息才能确定。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""
task_prompt["single_table_single_chart_multihop"]= """
你是一个专业的复杂问答数据合成专家，擅长基于单个表格与单个图表构造多跳推理问题。

### 输入
{{{graph_context}}}

### 任务类型
单表格 + 单 chart 多跳问答：需要在表格与图表之间多步往返推理。

### 生成要求
1. 问题设计：
   - 问题至少包含两步以上推理，例如：
     - 先在表格中根据静态条件筛选候选实体；
     - 再到图表中比较这些实体在时间维度上的表现；
     - 可能再回表格查看另一个属性后做最终选择。

2. 推理过程：
   - reasoning_path 明确描述：先用表格做什么筛选，再用图表做什么比较或验证，最后如何回到表格确定答案。

3. 答案要求：
   - 答案是表格中唯一实体的名称或属性值。
   - 既依赖表格也依赖图表，多步推理缺一不可。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""
task_prompt["single_table_single_formula"]= """
你是一个专业的复杂问答数据合成专家，擅长结合单个表格与单个公式设计问题。

### 输入
{{{graph_context}}}

### 任务类型
单表格 + 单公式问答：需要同时利用表格数据和公式进行推理，但不强制多跳。

### 生成要求
1. 问题设计：
   - 问题需要使用公式对表格中的实体进行计算或判断；
   - 但整体逻辑可以是一两步的直接应用。

2. 推理过程：
   - reasoning_path 描述：
     - 如何从表格中取出相关数据；
     - 如何代入公式进行计算或比较；
     - 如何据此选出实体。

3. 答案要求：
   - 答案是表格中某个实体的名称或其基于公式计算得到的属性值。
   - 唯一且明确。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""
task_prompt["single_table_single_formula_multihop"]= """
你是一个专业的复杂问答数据合成专家，擅长基于单个表格与单个公式构造多跳推理。

### 输入
{{{graph_context}}}

### 任务类型
单表格 + 单公式 多跳问答：在表格与公式之间进行多步推理。

### 生成要求
1. 问题设计：
   - 至少两步以上推理，例如：
     - 先根据表格中的某列或某些条件过滤实体；
     - 再对候选实体使用公式计算指标；
     - 最后按照计算结果选择最终实体。

2. 推理过程：
   - reasoning_path 描述从初筛到公式计算再到最终判断的完整链条。
   - 避免只描述“代入公式算一下”这种单步过程。

3. 答案要求：
   - 答案是表格中唯一满足多步条件的实体名称或属性值。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""
task_prompt["single_chart_single_formula"] = """
你是一个专业的复杂问答数据合成专家，擅长结合单个图表与单个公式设计问题，并将答案落在表格实体上。

### 输入
{{{graph_context}}}

### 任务类型
单 chart + 单公式问答：问题必须同时用到图表和公式，但不强制多跳。

### 生成要求
1. 问题设计：
   - 图表提供某些近似数值或趋势；
   - 公式提供计算规则；
   - 问题需要先从图表中读取量，再代入公式计算或判断，最终选出某条线/类别对应的实体。

2. 推理过程：
   - reasoning_path 描述：
     - 如何从图表获取相关数据；
     - 如何将这些数据应用于公式；
     - 如何根据结果选择对应实体。

3. 答案要求：
   - 答案是表格中某个实体的名称或属性值。
   - 必须同时依赖图表和公式才能确定。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""
task_prompt["single_chart_single_formula_multihop"]= """
你是一个专业的复杂问答数据合成专家，擅长基于单个图表与单个公式构造多跳推理问题，并将答案落在表格实体上。

### 输入
{{{graph_context}}}

### 任务类型
单 chart + 单公式 多跳问答：在图表和公式之间进行多步推理。

### 生成要求
1. 问题设计：
   - 至少两步以上推理，例如：
     - 先在图表中根据某一时间段或区间读出多个数据点；
     - 再将这些数据按公式计算中间指标；
     - 最后根据指标在实体之间做比较，选出答案。

2. 推理过程：
   - reasoning_path 顺序清晰地呈现：
     - 如何观察图表；
     - 如何计算中间量；
     - 如何据此筛选实体。

3. 答案要求：
   - 答案是表格中唯一符合多步图表+公式推理结果的实体名称或属性值。

4. 输出格式：
   - 严格返回 JSON，仅包含 question / answer / reasoning_path。
"""

